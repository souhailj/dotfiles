# dotfiles

## Installation

```bash
git clone git@github.com:souhailj/dotfiles.git ~/dotfiles
cd ~/dotfiles && ./install.sh
```

Sur Linux, ajouter à la fin du `.bashrc` :

```bash
eval "$(starship init bash)"
```
